<div class="form-group plat-form-title platdesigncomp" platcompname="${FORM_TITLE}" compcode="${FORMCONTROL_COMPCODE}"
  id="${CONTROL_ID!''}" 
  compcontrolid="${FORMCONTROL_ID}" platcomid="${FORMCONTROL_ID}" uibtnsrights="edit,del"
>
    <span class="plat-current">
		${FORM_TITLE}
	</span>
</div>